# Указываем язык прогаммирования
FROM "node:18.18"

#Указываем где открывать терминал
WORKDIR /pay_system

#Копируем все файлы/папки в Докер
COPY . /pay_system

#Установка зависмиости
RUN npm install

# указываем/бранируем порт для работы
EXPOSE 3000

#Команда для
CMD ["npm", "start"]